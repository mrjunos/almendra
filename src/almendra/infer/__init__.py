"""Reference inference pipeline (Phase 6).

Planned modules
---------------
  pipeline.py  load the ONNX model, run multi-view inference, and emit a
               per-bean accept/reject decision — the contract the physical
               sorting machine integrates against.
"""
