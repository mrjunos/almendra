"""Local Streamlit UI for the almendra toolkit (Phase 6).

A non-technical user can prepare data (tray capture + visual cropping), train a
model with live charts, evaluate it, and predict on a single bean — all without
touching the CLI. Launched via ``almendra ui`` or ``make ui``.
"""
